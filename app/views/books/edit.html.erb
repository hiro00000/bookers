<style>
  h4,p{
    margin-top: 10px;
  }
  .box{
    width: 450px;
    height:116px;
    border: 2px solid red;
    padding: 7px;
    padding-bottom: 0;
    margin-bottom: 20px;
    background-color: #f0f0f0;
  }
  .errors{
    text-align: left;
    font-weight: bold;
    padding: 5px 5px 5px 15px;
    font-size: 12px;
    margin: -7px;
    margin-bottom: 0;
    background-color: #c00;
    color: #fff;
  }
</style>

  <% if @books.errors.any? %>
  <div class="box">
    <p class="errors"><%= @books.errors.count %>errors prohibited this book from being saved:</p>
    <p class="message"><% @books.errors.full_messages.each do |message| %>
        <p>・<%= message %></p>
      <% end %>
    </p>
  </div>
  <% end %>

<h1>Editing Book</h1>
<%= form_with model: @books, url: book_path(@books.id), method: :patch do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  
  <h4>Body</h4>
  <%= f.text_area :body %>
  
  <p><%= f.submit 'Update Book' %></p>
  
  <p>
    <%= link_to "Show",book_path(@books.id) %> | <%= link_to "Back",books_path %>
  </p>
  
<% end %>